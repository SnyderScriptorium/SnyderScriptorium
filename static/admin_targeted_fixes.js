(function () {
  'use strict';

  // Keep the unauthenticated login as the only interactive layer until login succeeds.
  function protectAdminLogin(){const login=document.getElementById('login'),password=document.getElementById('password'),dashboard=document.getElementById('dashboard');if(!login||!password)return;const loggedIn=login.classList.contains('hidden');if(!loggedIn){if(dashboard){dashboard.style.display='none';dashboard.setAttribute('inert','');dashboard.setAttribute('aria-hidden','true');}login.style.position='relative';login.style.zIndex='2147483647';login.style.pointerEvents='auto';login.style.userSelect='text';password.style.position='relative';password.style.zIndex='2147483647';password.style.pointerEvents='auto';password.style.userSelect='text';password.style.touchAction='auto';password.removeAttribute('readonly');password.removeAttribute('disabled');password.tabIndex=0;login.querySelectorAll('input,button').forEach(el=>{el.style.pointerEvents='auto';el.style.userSelect='text';el.removeAttribute('disabled');});}else if(dashboard){dashboard.style.removeProperty('display');dashboard.removeAttribute('inert');dashboard.removeAttribute('aria-hidden');}}
  function wireLogin(){protectAdminLogin();const login=document.getElementById('login'),password=document.getElementById('password');if(!login||!password||login.dataset.loginProtected==='1')return;login.dataset.loginProtected='1';password.addEventListener('focus',()=>protectAdminLogin(),true);password.addEventListener('mousedown',()=>{protectAdminLogin();requestAnimationFrame(()=>password.focus());},true);password.addEventListener('click',()=>{protectAdminLogin();password.focus();},true);password.addEventListener('keydown',event=>event.stopPropagation(),true);password.addEventListener('input',()=>{password.removeAttribute('disabled');password.removeAttribute('readonly');},true);new MutationObserver(protectAdminLogin).observe(login,{attributes:true,attributeFilter:['class'],childList:true,subtree:true});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wireLogin,{once:true});else wireLogin();
  const KW_SUBCATEGORIES=['kwsnyderwriting','kw_short_stories','kw_poems','kw_vignettes'];let rememberedKWSubcategory='kwsnyderwriting';
  function setPostEditorCategoryMode(){const category=document.getElementById('postCategory'),wrapper=document.getElementById('postKWSubcategoryWrap'),sub=document.getElementById('postKWSubcategory');if(!category||!wrapper||!sub)return;const oldValue=category.value,oldKW=KW_SUBCATEGORIES.includes(oldValue)?oldValue:(oldValue==='journal'?'kwsnyderwriting':null);category.innerHTML='';[['curations','Book Curations'],['reviews','Book Reviews'],['curiosity','Curiosity Cabinet'],['kwsnyderwriting','K. W. Snyder Writing']].forEach(([value,label])=>{const option=document.createElement('option');option.value=value;option.textContent=label;category.appendChild(option)});category.value=oldKW?'kwsnyderwriting':(oldValue==='journal'?'kwsnyderwriting':(oldValue||'curations'));if(oldKW&&oldKW!=='kwsnyderwriting')rememberedKWSubcategory=oldKW;sub.value=KW_SUBCATEGORIES.includes(rememberedKWSubcategory)?rememberedKWSubcategory:'kwsnyderwriting';function sync(){const isKW=category.value==='kwsnyderwriting';wrapper.classList.toggle('hidden',!isKW);if(isKW){if(!KW_SUBCATEGORIES.includes(sub.value))sub.value=rememberedKWSubcategory||'kwsnyderwriting';rememberedKWSubcategory=sub.value;}const access=document.getElementById('postAccess');if(access){access.value=isKW?'members':'public';access.disabled=isKW;}}category.addEventListener('change',sync);sub.addEventListener('change',()=>{rememberedKWSubcategory=sub.value;sync();});sync();}
  function preserveKWSubcategoryAfterDraftLoad(){const category=document.getElementById('postCategory'),wrapper=document.getElementById('postKWSubcategoryWrap'),sub=document.getElementById('postKWSubcategory');if(!category||!wrapper||!sub)return;const apply=()=>{const value=String(category.value||''),isKW=KW_SUBCATEGORIES.includes(value)||value==='journal';if(isKW){const normalized=value==='journal'?'kwsnyderwriting':value;category.value='kwsnyderwriting';if(normalized!=='kwsnyderwriting')rememberedKWSubcategory=normalized;if(!KW_SUBCATEGORIES.includes(rememberedKWSubcategory))rememberedKWSubcategory='kwsnyderwriting';sub.value=rememberedKWSubcategory;wrapper.classList.remove('hidden');const access=document.getElementById('postAccess');if(access){access.value='members';access.disabled=true;}}else if(category.value){wrapper.classList.add('hidden');const access=document.getElementById('postAccess');if(access)access.disabled=false;}};apply();const observer=new MutationObserver(apply);observer.observe(category,{attributes:true,childList:true,subtree:true});observer.observe(sub,{attributes:true,childList:true,subtree:true});let last='';const poll=()=>{const sig=category.value+'|'+sub.value;if(sig!==last){last=sig;apply();}};const timer=setInterval(poll,150);setTimeout(()=>clearInterval(timer),15000);window.addEventListener('beforeunload',()=>clearInterval(timer),{once:true});}
  function removeDuplicateSizeSelectors(){document.querySelectorAll('.toolbar').forEach(toolbar=>{const sizes=Array.from(toolbar.querySelectorAll('select')).filter(select=>{const first=select.options&&select.options[0];return first&&String(first.textContent||'').trim().toLowerCase()==='size';});sizes.slice(1).forEach(select=>select.remove());});}
  function categoryForCard(card){const text=String(card.textContent||'').toLowerCase();if(text.includes('book curations'))return'curations';if(text.includes('book reviews'))return'reviews';if(text.includes('curiosity cabinet'))return'curiosity';if(text.includes('k. w. snyder writing')||text.includes('k.w. snyder writing')||text.includes('short stories')||text.includes('poems')||text.includes('vignettes'))return'kwsnyderwriting';return'other';}
  function enforcePublishedDisplay(){const list=document.getElementById('publishedList');if(!list)return;Array.from(list.children).forEach(card=>{if(!card.classList||!card.classList.contains('card'))return;if(categoryForCard(card)==='kwsnyderwriting'){const small=card.querySelector('small');if(small){const bits=small.textContent.split(' · ');if(bits.length)bits[bits.length-1]='Members Only';small.textContent=bits.join(' · ');}}});}
  function addPublishedFilters(){const section=document.getElementById('published'),list=document.getElementById('publishedList');if(!section||!list||document.getElementById('publishedFilters'))return;const wrap=document.createElement('div');wrap.id='publishedFilters';wrap.className='actions';wrap.style.marginBottom='8px';const title=document.createElement('strong');title.textContent='Show:';title.style.color='var(--brown)';wrap.appendChild(title);const filters=[['all','All'],['curations','Book Curations'],['reviews','Book Reviews'],['curiosity','Curiosity Cabinet'],['kwsnyderwriting','K. W. Snyder Writing']];let active='all';function apply(){enforcePublishedDisplay();const cards=Array.from(list.children).filter(card=>card.classList&&card.classList.contains('card')),counts={all:cards.length,curations:0,reviews:0,curiosity:0,kwsnyderwriting:0};cards.forEach(card=>{const c=categoryForCard(card);if(counts[c]!==undefined)counts[c]++;});cards.forEach(card=>card.classList.toggle('hidden',active!=='all'&&categoryForCard(card)!==active));wrap.querySelectorAll('button[data-filter]').forEach(button=>{const value=button.dataset.filter;button.className=value===active?'':'light';button.textContent=`${button.dataset.label} (${counts[value]||0})`;});}filters.forEach(([value,label])=>{const button=document.createElement('button');button.type='button';button.dataset.filter=value;button.dataset.label=label;button.textContent=`${label} (0)`;button.className=value===active?'':'light';button.addEventListener('click',()=>{active=value;apply();});wrap.appendChild(button);});const note=section.querySelector('.note');if(note)note.insertAdjacentElement('afterend',wrap);else section.insertBefore(wrap,list);const observer=new MutationObserver(apply);observer.observe(list,{childList:true});apply();}
  function addSubscriberDashboardLink(){const tabs=document.querySelector('.tabs');if(!tabs||document.getElementById('subscriberDashboardLink'))return;const button=document.createElement('button');button.id='subscriberDashboardLink';button.type='button';button.className='light';button.textContent='Subscriber Dashboard';button.addEventListener('click',()=>{location.href='/admin/subscribers';});const analyticsTab=document.getElementById('tab-stats');if(analyticsTab)analyticsTab.insertAdjacentElement('afterend',button);else tabs.appendChild(button);}
  function addTodayAnalyticsButton(){const stats=document.getElementById('stats');if(!stats||document.getElementById('analyticsTodayButton'))return;const actions=stats.querySelector('.actions');if(!actions)return;const b=document.createElement('button');b.id='analyticsTodayButton';b.type='button';b.className='light';b.textContent='1 Day — Today';b.onclick=()=>window.loadStats&&window.loadStats('day');actions.insertBefore(b,actions.firstChild);const three=stats.querySelector('.three');if(three&&!document.getElementById('statUniqueVisitors')){const card=document.createElement('div');card.className='card';card.innerHTML='<div><h3>Unique Visitors</h3><small>Selected period</small></div><strong id="statUniqueVisitors">0</strong>';three.appendChild(card);}}
  function normalizeAnalyticsLabel(category,path){const c=String(category||'').toLowerCase();if(path==='/')return'Home';if(c==='site')return'Home';if(c==='journal')return'K. W. Snyder Writing';return({curations:'Book Curations',reviews:'Book Reviews',curiosity:'Curiosity Cabinet',kwsnyderwriting:'K. W. Snyder Writing',kw_short_stories:'K. W. Snyder Writing — Short Stories',kw_poems:'K. W. Snyder Writing — Poems',kw_vignettes:'K. W. Snyder Writing — Vignettes'})[c]||category||'Home';}
  function escAnalytics(value){const d=document.createElement('div');d.textContent=value==null?'':String(value);return d.innerHTML;}
  function installAnalyticsOverride(){window.loadStats=async function(period='30'){window.analyticsPeriod=period;try{if(typeof window.refreshCounts==='function')await window.refreshCounts();const r=await fetch(`/api/analytics-v3?period=${encodeURIComponent(period)}`,{credentials:'same-origin'});if(!r.ok)throw new Error(`Analytics request failed (${r.status})`);const d=await r.json();const statViews=document.getElementById('statViews');if(statViews)statViews.textContent=d.total_views_today??d.total_views??0;const unique=document.getElementById('statUniqueVisitors');if(unique)unique.textContent=d.unique_visitors??0;const cards=document.getElementById('analyticsCategories');if(cards){const map={};(d.content_views||[]).forEach(x=>{const label=normalizeAnalyticsLabel(x.category,x.path);map[label]=(map[label]||0)+Number(x.views||0);});cards.innerHTML=Object.entries(map).sort((a,b)=>b[1]-a[1]).map(([label,views])=>`<div class="card"><span>${escAnalytics(label)}</span><strong>${views}</strong></div>`).join('')||'<p class="note">No section views yet.</p>';}if(typeof window.renderAnalytics==='function')window.renderAnalytics({daily:(d.daily_views||[]).map(x=>({day:x.day,views:x.views})),categories:[],posts:(d.content_views||[]).map(x=>({title:x.title,category:normalizeAnalyticsLabel(x.category,x.path),views:x.views}))});const heading=document.querySelector('#stats .preview h3');if(heading)heading.textContent=period==='day'?'Views by Hour — Today':'Views Over Time';const totalCard=statViews&&statViews.closest('.card');if(totalCard){const h3=totalCard.querySelector('h3'),small=totalCard.querySelector('small');if(h3)h3.textContent=period==='day'?'Total Views Today':'Total Views';if(small)small.textContent=period==='day'?'Today':'Selected period';}}catch(e){if(typeof window.showStatus==='function')window.showStatus(e.message,true);else console.error(e);}};}
  function installKWPublishGuard(){const canonical=function(){const c=document.getElementById('postCategory'),sub=document.getElementById('postKWSubcategory');let value=(c&&c.value)||'';if(value==='journal')value='kwsnyderwriting';if(value==='kwsnyderwriting'&&sub&&sub.value)value=sub.value;if(['kw_short_stories','kw_poems','kw_vignettes'].includes(value))return value;if(value==='kwsnyderwriting')return'kwsnyderwriting';return value;};window.effectivePostCategory=canonical;const access=document.getElementById('postAccess'),category=document.getElementById('postCategory');if(category&&access){const sync=()=>{const value=canonical(),locked=value==='kwsnyderwriting'||value.startsWith('kw_');access.value=locked?'members':'public';access.disabled=locked;};category.addEventListener('change',sync);if(document.getElementById('postKWSubcategory'))document.getElementById('postKWSubcategory').addEventListener('change',sync);sync();}}
  async function repairLegacyJournalContent(){try{const posts=await fetch('/api/published',{credentials:'same-origin'}).then(r=>r.ok?r.json():[]);for(const p of posts){if(p.category!=='journal')continue;await fetch(`/api/published/${p.id}`,{method:'PUT',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:p.title,content:p.content,category:'kwsnyderwriting',accessLevel:'members'})});}const drafts=await fetch('/api/drafts',{credentials:'same-origin'}).then(r=>r.ok?r.json():[]);for(const d of drafts){if(d.category!=='journal')continue;await fetch(`/api/drafts/${d.id}`,{method:'PUT',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:d.title,content:d.content,category:'kwsnyderwriting',date:d.date})});}}catch(_){} }
  function start(){wireLogin();setPostEditorCategoryMode();preserveKWSubcategoryAfterDraftLoad();removeDuplicateSizeSelectors();addPublishedFilters();addSubscriberDashboardLink();addTodayAnalyticsButton();installKWPublishGuard();repairLegacyJournalContent();setTimeout(removeDuplicateSizeSelectors,50);setTimeout(removeDuplicateSizeSelectors,250);setTimeout(installAnalyticsOverride,0);setTimeout(installAnalyticsOverride,100);}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();